function runAlgorithm(segments) {

    //sort segments

    //double for loop: i from 0 to n, and j from i to n
    //inside these two loops, process i,j to i,n while deleting/redirecting lines sometimes

}

function sortOnLength(segments) {

    return segments;
}
